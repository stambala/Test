spring :
  application :
    name : Test
  
  h2 :
    console : 
      enabled : true
   
  datasource :
    url : jdbc:h2:~/data/test
    userName : sa
    password : sa
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: update